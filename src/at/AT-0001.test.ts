it('GIVEN a valid config file, WHEN the user runs "feed-enhancer --c path/to/conf.yaml --i path/to/input_dir --o path/to/output_dir", THEN the CLI exits with code 0', () => {
  // GIVEN a valid config file
  // TODO
  // WHEN the user runs "feed-enhancer --c path/to/conf.yaml --i path/to/input_dir --o path/to/output_dir"
  // TODO
  // THEN the CLI exits with code 0
  // TODO
})
